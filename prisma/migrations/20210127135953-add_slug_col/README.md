# Migration `20210127135953-add_slug_col`

This migration has been generated by lmjcbs at 1/27/2021, 1:59:53 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "categories" ADD COLUMN     "slug" TEXT NOT NULL DEFAULT E''

ALTER TABLE "locations" ADD COLUMN     "slug" TEXT NOT NULL DEFAULT E''

ALTER TABLE "tags" ADD COLUMN     "slug" TEXT NOT NULL DEFAULT E''
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210126122945-base_models..20210127135953-add_slug_col
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -50,8 +50,9 @@
 model Category {
   id        Int      @id @default(autoincrement())
   name      String   @unique
   jobs      Job[]
+  slug      String   @default("")
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   @@map(name: "categories")
@@ -78,8 +79,9 @@
 model Location {
   id        Int      @id @default(autoincrement())
   name      String   @unique
   jobs      Job[]
+  slug      String   @default("")
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   @@map(name: "locations")
@@ -88,8 +90,9 @@
 model Tag {
   id        Int      @id @default(autoincrement())
   name      String   @unique
   jobs      Job[]    @relation(references: [id])
+  slug      String   @default("")
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   @@map(name: "tags")
@@ -103,5 +106,4 @@
   updatedAt DateTime @updatedAt
   @@map(name: "types")
 }
-
```


